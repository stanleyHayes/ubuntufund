node_modules
dist
.turbo
coverage
*.log
.env
.env.local
.env.*.local
.env.*
!.env.example
# Frontend production build config is public (client bundle, no secrets) — tracked.
!apps/web/.env.production
!apps/admin/.env.production
!apps/marketing/.env.production
credentials.txt
.DS_Store

# Local tool state
.expo
.raven

# Commercial font files (TT Squares) - keep out of the repo
tt-squares/
*.tsbuildinfo
