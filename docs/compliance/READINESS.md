# Ghana and mobile store compliance readiness

Audit date: 12 September 2026. Owner: Codex. Status: **IN PROGRESS — NOT RELEASE APPROVED**.

This ledger covers the implemented donation/reward crowdfunding, creator gifts, subscriptions, wallet, crypto, UGC/live sessions, KYC, and personal-data flows across API, web, marketing, admin and native apps. A source-code check does not establish a licence, nonprofit status, privacy registration, legal approval, correct production configuration or store approval. Requirements for additional distribution countries must be added once the owner confirms launch territories. No assumptions of worldwide legal compliance are made.

## Financial accounting checkpoint — 13 September 2026

Donation success, journal/projections and outbox commit together. Split accrual consumes the exact locked version with all stored beneficiary consents. Wallet contributions also debit the donor and record history in that transaction, with owner-bound retries and no compensation after an uncertain commit. Focused failure/concurrency evidence and remaining historical repair/current eligibility/provenance gates are tracked in `DONATION_SETTLEMENT_INTEGRITY.md`.

Full API baseline de8b9e2 passes 1,210 tests across163 files (session41412, exit0, 1,252.56seconds; `/tmp/ujimora-current-accounting-full-regression.log`). This includes the split-consent, wallet transaction, required campaign projection and historical audit changes. This is engineering evidence, not regulated-payment authorization or provider/store release approval.

## Current frontend verification — 13 September 2026

Current implementation passes all395 frontend tests: web203 across48 files, admin73 across21, marketing10 across4 and native109 across23. Native Android/iOS/web JavaScript export also succeeds. Evidence logs: `/tmp/ujimora-{web,admin,marketing}-85314b7-tests.log`, `/tmp/ujimora-mobile-1a43ae5-tests.log`, `/tmp/ujimora-mobile-1a43ae5-export.log`. Full API session45526 is still running against1a43ae5 source; earlier interrupted run25869 is not a pass. These checks do not close the external legal/provider/store or signed-native release gates below.

Production builds for web/admin/marketing also pass on this implementation, and all14 workspace type/lint tasks pass uncached (zero lint errors; existing warnings remain). Logs `/tmp/ujimora-{web,admin,marketing}-ceb9304-build.log` and `/tmp/ujimora-ceb9304-workspace-checks.log`. Build success is not deployment or signed-artifact verification.

## Current authoritative references

| ID | Source | Applicability |
| --- | --- | --- |
| GH-DP | [Data Protection Commission compliance guidance](https://dataprotection.org.gh/wp-content/uploads/2025/07/GUIDELINES-TO-DEMONSTRATE-DATA-PROTECTION-COMPLIANCE-1.pdf), Data Protection Act 2012 (Act 843) | Personal information, lawful purpose, notice, rights, security, processors, registration. Registration is an owner gate. |
| GH-ET | [Electronic Transactions Act 2008 (Act 772)](https://www.csa.gov.gh/resources/Electronic%20Transactions%20Act.pdf) | Online supplier/contract/price disclosures and applicable consumer rights; review transaction-specific exceptions before applying cancellation rules to donations. |
| GH-CF | [Bank of Ghana crowdfunding framework](https://www.bog.gov.gh/fintech-innovation/innovation-hub/crowdfunding/), [current policy publication](https://www.bog.gov.gh/news/crowdfunding-policy/) | Donation/reward collection, holding and disbursement require the appropriate approved regulated structure; a Paystack API integration does not prove authorization. |
| GH-IDV | [Revised Ghana Card notice](https://www.bog.gov.gh/notice/revised-ghana-card-notice-for-financial-transactions/), [November 2025 supervisory guidance](https://www.bog.gov.gh/news/supervisory-guidance-note/) | Conditional regulated identity/liveness and financial-channel authentication requirements. Applicability and provider responsibility remain open; see `RFI_KYC_APPLICABILITY.md`. |
| GH-AML | [Anti-Money Laundering Act 2020 (Act 1044)](https://fic.gov.gh/AML%20ACT%202020%20%28ACT%201044%29.pdf), [FIC guidance](https://fic.gov.gh/index.php/publications/faq) | Confirm accountable-institution/partner obligations, due diligence, risk escalation, reporting and restricted record retention with qualified reviewers. Do not fabricate FIC reports. |
| GH-VA | [Bank of Ghana virtual assets](https://www.bog.gov.gh/virtual-assets/) | Act 1154 (2025) and developing implementation apply where the service performs regulated virtual-asset activities. Crypto provider capability is not legal authorization. |
| GH-SEC | [SEC investment crowdfunding guidelines](https://sec.gov.gh/wp-content/uploads/Final-Regulatory-Laws/Guidelines/SEC_Crowdfunding_Guidelines_2024.pdf) | Conditional: equity/debt/investment features are prohibited by current product terms; no investment permissions inferred. |
| GH-TAX | [GRA gift tax](https://gra.gov.gh/portfolio/gift-tax/), [GRA donation practice note](https://gra.gov.gh/wp-content/uploads/2020/09/Practice-Note-on-Contribution-or-a-Donation-to-a-Worthwhile-Cause.pdf) | Ordinary payment receipts must not imply charitable tax deductibility; company tax/VAT/withholding applicability requires accountant review. |
| APPLE | [App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/) | UGC 1.2, payments 3.1, fundraising 3.2.1/3.2.2, privacy 5.1, legal identity/content rights, accurate metadata. Read native fundraising separately from digital subscriptions. |
| PLAY-PAY | [Google Play Payments](https://support.google.com/googleplay/android-developer/answer/9858738?hl=en) | Store billing for digital features; defined donation/P2P exceptions do not automatically cover creator fees or digital entitlements. No regional billing exception assumed. |
| PLAY-DELETE | [Google account deletion](https://support.google.com/googleplay/android-developer/answer/13327111?hl=en) | In-app and web deletion paths, associated-data deletion, disclosed justified retention, processor follow-up. |
| PLAY-UGC | [Google user-generated content](https://support.google.com/googleplay/android-developer/answer/9876937?hl=en-GB) | Terms acceptance before posting, report content/users and block users, moderation. |
| PLAY-TARGET | [Target API requirements](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en) | Current submissions require Android API 36; validate generated manifest and release artifact, not only config. |

## Work and evidence ledger

Statuses: AUDITING, IN PROGRESS, ENGINEERING VERIFIED, EXTERNAL GATE, NOT APPLICABLE (with justification). Engineering verification requires tests or direct observable evidence. External gates remain open until an actual artifact/reference is reviewed.

| ID | Requirement / acceptance | State | Current evidence / next action |
| --- | --- | --- | --- |
| C01 | Correct legal operator, seller, registration, address and policy contacts | EXTERNAL GATE | `packages/types/src/legal.ts` identifies DevTrack BN843072020; user identifies Neurodyne Corp Ltd as parent. Requested authoritative relationship/identity; do not silently overwrite registered facts. |
| C02 | DPC registration, privacy owner, processor agreements and transfer assessment | EXTERNAL GATE | Requested registration evidence. Existing policy promises must be reconciled with actual documented controls. |
| C03 | BoG/provider crowdfunding authorization; wallet safeguarding and reconciled payout structure | EXTERNAL GATE | Requested authorization/partner evidence; technical wallet functionality does not prove permission to hold funds. |
| C04 | Current policy/age acknowledgement captured and enforced before UGC | IN PROGRESS | Web/native signup captures unchecked terms/18+ confirmation with server timestamp and version 2026-09-12; existing-account acknowledgement and API write gate implemented. Legal-acceptance integration tests pass. Guest donation/creator messages now require unchecked current terms/18+ acknowledgement on web/native campaign checkout, including crypto. Use cases reject missing acknowledgement before payment creation and persist the server-stamped record with the intent/tip. Fiat/creator/crypto regressions and a phone consent flow pass; see `CONTENT_MESSAGES.md`. Initial signed-in fiat/creator/crypto messages now enforce current publishing restrictions; lookup failures fail visibly rather than falling back to guest processing. Five message/session tests plus API type/lint pass. Organization-team update/profile writes now enforce the same agreement/restriction guard, including mixed-case routes (7 regression tests pass). Remaining: every other UGC entry point and complete moderation projection coverage. |
| C05 | Separate opt-in for marketing, push and website contact; withdrawal | IN PROGRESS | Website request remains unchecked and organization-only when URL is blank, now also on mobile signup. Web/native signed-in notices support authenticated withdrawal; server timestamps new requests and first withdrawal, and stale account updates cannot restore the flag. Shared privacy notice identifies the recipient/purpose. Seven API and six web tests plus API/web/native type and lint checks pass. A 390px browser flow verifies links, withdrawal and reload persistence; its screenshot was inspected. Activity notifications now have separate default-off category/channel choices, verified-email gating, durable capture/delivery, consent rechecks and account-closure suppression. Category/concurrency/retry tests, web Settings tests, a phone browser flow and mobile export pass; see `ACTIVITY_ALERTS.md`. Legacy request timestamps remain unknown. Newsletter confirmation and withdrawal now share one web/native preference: legacy addresses are excluded, confirmation mail is encrypted, withdrawal suppresses queued delivery, and erasure cleans up consent records. Eight API, seven component and one phone browser tests pass; all five app type/lint checks pass. See `NEWSLETTER.md` for campaign-tooling, retention and provider gates. Push/SMS/announcement switches advertised services without senders. These are now marked unavailable; native Settings no longer requests push permission, and new push registration rejects without collecting identifiers. Legacy unregister remains available. See `PUSH_NOTIFICATIONS.md` for future delivery/consent gates. Downstream website-contact suppression and retention remain open. |
| C06 | Discoverable public web deletion resource in both site shells | ENGINEERING VERIFIED | Shared /delete-account policy renders in web, marketing and native, with authenticated settings and public email request paths. Four browser legal tests pass including narrow layout and actionable deletion links. Production URL publication/store-console entry remains C19. |
| C07 | Account deletion removes associated personal data, revokes sessions, handles processors and justified retention | IN PROGRESS | Persisted account closure now rejects old access/refresh tokens across instances; retryable core cleanup removes profile, push/reset tokens, newsletter/notifications and creator profile, hides comments/donor identity, ends live sessions and queues restricted residual-data review. Admin privacy queue supports retry, notes and next review dates. Tests pass for deletion, financial preservation, retry and current-role access. Processor erasure and category-specific retention remain open. |
| C08 | Data access/export, correction and complaint flow; documented retention by category | IN PROGRESS | Web/native Settings now submit and track private access/correction/complaint requests; admin queue, fixed 30-day operational target, private review evidence, revision fencing and transactional audit history are implemented. Responses appear in Settings and can be downloaded/shared. Closed-account external delivery requires operator-recorded identity/delivery evidence. This is a reviewed request process, not an automatic all-data export. Six API, four component and a phone download flow pass; mobile 42 tests and all-platform export pass. Category inventory, processor access and unresolved retention periods/holds are documented in `DATA_RIGHTS.md`; complete fulfilment and approved retention jobs remain open. |
| C09 | UGC report content/users, blocking, moderation and timely escalation | IN PROGRESS | Persistent block/unblock and reporting controls now cover discussions and creator profiles on web/native, with unblock lists in Settings. API filters blocked comments/supporter messages, prevents blocked creator interaction, preserves totals and hides closed/restricted creator pages. Admin safety queue snapshots evidence, prioritizes urgent reports, records reviews, hides comments and applies/appeals publishing restrictions while preserving settings/financial access. Focused API/UI tests and a 390px browser flow pass. Live read/token guards, identifiable viewer eviction, moderator provider-stop actions and retryable cleanup are implemented. Individual donor/supporter reporting and hiding now work on web/native with private snapshots and financial totals preserved; anonymous tip IDs do not expose author identity. Current-message checks suppress stale outbox and buffered SSE text. Focused API/replay/component/admin and phone checks pass; see `CONTENT_MESSAGES.md`. Campaign-update reports now capture private versioned evidence; staff hiding/restriction is retryable and rejects stale reviewed content without changing funds. Nonpublic update reads, viewer caches and blocked-author filters are covered by API/UI/phone checks. Comment/update preventive admission now uses explicit optional OpenAI consent, private pending versions, exact-version staff decisions and author review history; see `PUBLICATION_REVIEWS.md`. Campaign creation and vanity URL changes now use the same private version-bound admission, with current financial eligibility rechecked and slug-only persistence preserving concurrent balances/status. Campaign/legacy-publication/policy regressions pass 34 tests; the final campaign/unit run passes 13, four web/two admin review tests and 59 native tests pass, and the phone flow, type/lint/build and final native exports pass. Detailed full-run/correction evidence is in `PUBLICATION_REVIEWS.md`. Legacy campaign staff review now records complete-version evidence, current staff identity, notes, explicit content/fundraising attestations and transactional status/audit history. Account closure removes duplicated content snapshots, while decision provenance/funds remain. See `CAMPAIGN_STAFF_REVIEW.md`. Creator-page changes now have full merged-version admission and transactional revision/credential fencing, with explicit reviewed images and a pause-only exception preserving funds; see `CREATOR_PUBLICATION.md` for verification status. Organization name/website edits now also have private reviewed versions, transactional delegation/credential/revision checks and scoped web/native forms; verified API/lifecycle/UI/build/export evidence is recorded in `ORGANIZATION_IDENTITY.md`. Account identity edits/private-to-public admission and private-contact preservation are verified in `ACCOUNT_PUBLICATION.md`. Public member and organization identity endpoints now exclude private/restricted/closed/blocked accounts, remove public login-email exposure and use noncacheable responses with viewer-scoped client refreshes. Focused API/browser checks pass; native and final artifact evidence is recorded in `ACCOUNT_PUBLICATION.md`. Remaining: legacy public-content rollout, registration, every identity projection and restricted-account read policy, payment/live/media preventive admission, provider-backed revocation/rollout, broader anonymous-author blocking and live-client update coverage, and staffed escalation. |
| C10 | Native digital subscription billing, restoration and server verification | IN PROGRESS | Expo IAP purchase/restore and store management replace native Paystack. Server verification, encrypted purchase ownership, transactional entitlements, durable notifications/acknowledgements and web/store checkout exclusion are implemented. Full API: 743 tests; mobile: 36 tests; web management regression and all-platform JavaScript exports pass. Sanitized admin recovery queues and audited retries are implemented. Deployment configuration and real signed-device store evidence remain; see STORE_BILLING.md. |
| C11 | iOS fundraiser collections use approved nonprofit/Apple Pay path or actual external browser | IN PROGRESS | iOS campaign donation route now opens the public website with Linking.openURL before donor entry or payment creation; wallet and crypto donation forms are not mounted on iOS. Only public slug/amount/live context is passed. Creator external checkout is disabled on native because the fee-bearing model does not establish the personal-gift exception; profiles and existing balances remain available. Mobile 42-test suite, type check and lint pass. Physical browser handoff and store/provider classification remain external checks. |
| C12 | Crypto permission, transparent conversion/network/refund disclosures and disabled-until-approved availability | AUDITING | Provider feature gates and legal disclaimers exist; review current Act 1154 requirements and authorization evidence. Read-only enablement/recovery trace in `CRYPTO_RELEASE_GATES.md` records the scheduled-recovery coupling fix and six passing disabled-intake/recovery tests; provider and remaining recovery gates stay open. |
| C13 | KYC/KYB, beneficiary authority, AML/risk escalation, sanctions/PEP evidence, audited holds | IN PROGRESS | Identity and organization evidence intake, private attachments, representative/control declarations, staff review findings, version-bound transactional decisions and applicant correction flows are implemented across API/web/native/admin. Expiry status and renewal are connected. Automatic payout selection and local authorization/reservation now enforce current evidence; revocation and committed-before-provider tests pass. Latest focused evidence and limits are in `KYC_REVIEW_INTEGRITY.md`; full API regression subsequently passes all 1,091 tests across 154 files at API baseline 69d94fa. Still open: other campaign/manual/creator/wallet/beneficiary privilege consumers, latest-record/legacy policies, provider identity/liveness/sanctions/PEP and staffed reporting/retention. Conditional regulated requirements remain in `RFI_KYC_APPLICABILITY.md`; staff model in `STAFF_ACCESS.md`. |
| C14 | Accurate pricing, fee/net review, receipts, refund/complaint access and no unsupported tax claims | IN PROGRESS | Removed automatic intake deductions and unsupported timing/price promises. Durable attempts, campaign/beneficiary funds holds, provider verification and transactional accounting recovery implemented; latest hold/race/breakdown checks pass 31 API/use-case tests, five admin/eight web tests and affected checks. Earlier mocked desktop/phone recovery flows pass; see `REFUND_RECOVERY.md`. Remaining payout lifecycle review, failed-case disposition, donor case linkage, legacy reconciliation, fee/tip/currency policy, receipts and approved terms remain open in `REFUNDS_AND_FEES.md`. |
| C15 | Sensitive data minimization, private KYC media, access controls, audit and incident response | IN PROGRESS | New KYC uploads use Cloudinary authenticated delivery and opaque owner-bound references; API issues 60-second viewing links only to owner/current admin and logs access without URLs. Web/native/admin previews updated. Eight upload/KYC integration tests pass. Legacy public assets, incident assessment and provider/CDN invalidation remain release gates; see PRIVATE_KYC_ROLLOUT.md. |
| C16 | AI disclosure/consent and user-content safety | IN PROGRESS | Web/native require separate unchecked consent for each OpenAI writing request. API/service reject missing permission before quota/provider call; usage stores consent date/version/provider without raw input/output. The real adapter now screens input and generated drafts with OpenAI moderation, withholding flagged results and failing closed on unavailable/malformed screening. Twenty-one focused API/service/adapter tests pass. Web/native suggestions now have private in-app reports with original-output fingerprint verification and audited admin resolution; reporting a model output cannot restrict the reporter. See `AI_SAFETY.md`: staffed escalation/feedback, retention/response procedures, broader UGC screening and live provider/language evidence remain open. |
| C17 | Accurate public privacy notice, Data Safety/App Privacy inventory and SDK permission declarations | IN PROGRESS | Source-based feature/SDK inventory and reconciled submission checklist are in `STORE_DATA_INVENTORY.md` and `apps/mobile/STORE_SUBMISSION.md`. Final signed-artifact/network evidence, processor terms and console answers remain open. |
| C18 | API 36, current SDK/build requirements, permission minimization, encryption/accessibility | IN PROGRESS | Local Xcode 26.6 verified; Expo 55 Android module defaults target/compile SDK 36. Introspection verifies audio-only iOS background modes, no always-location declaration; optional device-biometric protection was subsequently implemented and needs final signed-artifact permission verification, and explicit Android overlay/background-location/broad-media permission removal. iOS/Android/web JavaScript export passes. Final signed manifest, linked SDK/16 KB libraries, physical-device permission denial and accessibility remain open. |
| C19 | Store declarations, territories, age rating, identity, review account, screenshots, privacy/deletion URLs | EXTERNAL GATE | Owner input requested; no store-console submission or approval authorized/implied by code implementation. |
| C20 | End-to-end acceptance and release gate | AUDITING | Run focused unit/integration, cross-app type/lint/build and browser/native flows after changes; keep physical-device/provider/store tests distinct. |
| C21 | Corporate, tax, employment and other non-software Ghana obligations | EXTERNAL GATE | Map relevant corporate/tax/consumer/security obligations with qualified reviewers. Software cannot constitute incorporation, filings, licence issuance or a legal opinion. |

## Execution rules

- Preserve existing customer funds, transaction evidence and unrelated local files. Use isolated test data; no real-money transactions or regulator/store submissions.
- Never mark soft deletion as full erasure, a provider flag as a licence, a KYC screen as completed due diligence, or a successful export as a signed store build.
- Recheck authoritative sources at release; store policies and regulatory implementation can change.
- Complete engineering work that can proceed independently of pending owner answers. Record exact blockers and evidence needed instead of inventing facts.

## Verification checkpoint — privacy implementation

- API, web, admin and native TypeScript checks pass; API lint passes.
- Focused registration/legal/deletion/private-upload/AI suites pass. Full API run initially passed 669/670; the remaining test incorrectly expected a promoted user's old token to retain non-admin permissions. It now uses a separate non-admin account; its rerun passes. Fresh full API run passes 99 files / 673 tests. Full web, admin and mobile unit suites pass 91, 27 and 26 tests respectively. Subsequent discussion-blocking changes are verified separately.
- No production personal data was migrated or deleted, no payment was made, and no regulator/store filing or external message was sent.
- Current-role and persisted-deletion checks are verified independently of process-local JWT revocation. Password changes and resets now persist a credential version checked by access, optional-auth and refresh paths. Stale profile saves cannot restore credentials, and reset-token consumption commits transactionally with the password. Seven authentication/closure tests and API type checks pass, including fresh instances and concurrent resets. Recovery now uses an encrypted expiring email queue and a public reset form; raw reset-link logging is removed. Initial API/UI/browser checks pass; see `ACCOUNT_EMAILS.md`. Email verification request/confirmation and password-change security messages now share the encrypted account-mail queue. Verification remains separate from opt-in and does not grant a session, role or KYC level. Nineteen account-email/session API tests, ten web tests, a phone browser flow, type/lint checks and native export pass. Historical log review and actual provider delivery remain operator gates.
- Privacy, UGC, native billing, financial permissions and store artifacts are separate remaining requirements. This checkpoint does not establish full compliance.

## Native permission and dependency checkpoint

- Native app uses system image/file selection, optional camera capture, foreground address location and live microphone/camera. No background location, Bluetooth scanning, external accessory, background-fetch task is implemented; optional local biometric session protection is now implemented (see the account-protection evidence below). Generated capabilities now omit those unused declarations; LiveKit audio and screen-broadcast extension remain.
- Configuration introspection is not a signed build. Local ignored `ios/` files must be regenerated from the committed plugins/config before the release archive; inspect the resulting Info.plist, entitlements and Android manifest. Normal visible push notifications do not require background remote-notification tasks in this implementation.
- Dependency audit dated 2026-09-12 found 32 affected package entries (22 moderate, 9 high, 1 critical), including transitive development tooling and runtime dependencies. The critical entry concerns the Vitest UI server; this run used `vitest run`, not the UI server. Remediation remains required and must distinguish direct vulnerability paths from affected parent packages. Do not blindly accept audit suggestions that downgrade Expo or React Navigation across major versions.
- Next engineering slices: UGC user/comment reporting and blocking; native store billing and fundraising navigation; privacy rights/retention/consent withdrawal; verified dependency updates and store release artifacts.

## Community safety implementation checkpoint

- Added private reporter intake with bounded reasons/details, duplicate-pending-report suppression and per-process rate limiting. Reported users cannot read the admin queue or reporter identity. Evidence snapshots remain available after comment removal.
- Publishing restrictions persist across API instances; review notes and appeal restoration are audited. Reports and restrictions do not delete balances or deny account settings, privacy requests or existing financial access. Public creator pages are hidden while restricted or after account closure.
- Focused tests: moderation/blocks/acceptance/action center: 9 passed; creator blocks and payout service regression: 12 passed; existing creator payment/withdrawal integration: 14 passed; web reporting/blocking interactions: 2 passed. Type checks pass for API/web/admin/native. Phone-width browser reporting/blocking flow passes and its screenshot was inspected.
- Initial full API privacy checkpoint remains 673 tests; later safety deltas have focused verification and still need the final full-release sweep. No test has sent a real report, notification, payment or moderation action to production.
- Remaining moderation operations and release evidence are in MODERATION_OPERATIONS.md. No claim of full UGC/store compliance is made from comment controls alone.

## Live safety implementation checkpoint

- Signed-in viewers receive stable, subscribe-only participant identities. Public stream reads, active-stream discovery, overlay access and new video tokens check account closure, publishing restrictions and user blocks. The viewer UI disconnects on denied access or a persisted block.
- Blocking enqueues durable provider cleanup. Moderation stops new access and invalidates overlay delivery before provider calls, revokes the host identity with an explicit LiveKit cutoff, then closes the room. Provider failure leaves the report pending and a retryable cleanup flag. Account deletion also queues its active rooms for cleanup.
- A 30-second reconciliation job and boot sweep retry outstanding work; admin action-center counts and the safety queue expose remaining work and a retry action. Previously issued rooms cannot be marked clean merely because provider credentials are missing.
- Report review reserves one immutable action before external effects. Conflicting actions receive 409; another process can retry the same action while preserving the original notes. Same-action effects are idempotent; audit delivery can be at least once.
- Focused live-video/live-session regression tests pass, provider stop/eviction/restart tests pass, and viewer disconnect tests pass. Tests use isolated data and provider doubles. Actual LiveKit Cloud token invalidation and production room cleanup remain external verification, not implied by a mocked provider.
- See MODERATION_OPERATIONS.md for provider-specific rollout constraints and remaining safety work.

## 12 September follow-up verification checkpoint

- Full API checkpoint: 107 files / 743 tests. Subsequent website withdrawal tests (7), billing ownership/API stale-failure and operator tests (23), and admin action-center regression pass. Later changes still require the final full-suite sweep.
- Mobile unit checkpoint: 42 tests; web website notice: 6 tests; admin billing recovery UI: 2 tests. API/web/mobile/admin type and lint checks pass for the affected slices.
- Website withdrawal is verified at 390px with mocked browser requests, including retry behavior in component tests and persistence across reload; screenshot inspected. API separately verifies durable withdrawal and stale account-save protection with real isolated MongoDB records.
- Fresh iOS/Android/web JavaScript export after native website consent and fundraising changes passes. An isolated CocoaPods installation is still downloading Hermes; native simulator compilation has not yet completed. No signed archive or real store purchase is claimed.
- No production data changes, parent-company contact, provider transaction, store submission or main push was performed in this checkpoint. The original website feature remains previously pushed; this broader compliance slice is still local and the goal remains active.

## 12 September dependency and store-declaration follow-up

- `DEPENDENCY_SECURITY.md` records pinned npm/Node requirements, compatible dependency updates, five consumer-level security checks, and exact-version install patches. The stable full audit reports nine entries associated with the two locally patched chains; it is not a clean upstream audit. The full API invocation yielded 796 passes and three consent-fixture failures; the corrected files plus consent regressions pass 16 tests. Final isolated clean install and five security checks pass. Expo Doctor passes 20/20 after SDK alignment.
- Eighteen type/lint/build tasks, 118 web tests, 32 admin tests, 8 marketing tests, 42 native tests and all-platform native export pass at this checkpoint. The public deletion resource is now in the marketing sitemap.
- `STORE_DATA_INVENTORY.md` maps current feature/SDK data flows to evidence needed for C17. `apps/mobile/STORE_SUBMISSION.md` has been reconciled with disabled push enrollment, hybrid erasure, native IAP, external iOS fundraising and remaining signed-device/operator requirements. Console answers and release approval remain open.

- Logging follow-up: route templates replace raw request URLs, structured URLs omit query/userinfo, error serializers discard raw payloads, and known sensitive fields are removed. Eighteen logging/auth/Paystack regressions pass; see `LOGGING_PRIVACY.md`. Historical log access, retention and incident review remain operator tasks.

## Campaign-update safety verification checkpoint

- Stable full API baseline before this slice: 120 files / 822 tests pass. Current update/team/agreement focused run: 11 tests; corrected mixed-case static route regression retains original IDs and passes with the 7-test team/agreement rerun. Six web report/cache tests, two admin moderation tests, 43 native tests, affected type/lint checks, web/admin builds and all-platform native export pass. Mocked 390px update-report flow passes and screenshot was inspected.
- Evidence is private; hide/restrict decisions bind the original full update digest and preserve funds. A failed later audit can retry the original action, while edits invalidate stale hides. No real reports, provider effects or main push occurred in this checkpoint. Preventive publication screening remains open.

- Comment privacy follow-up: draft/pending/blocked campaign comments now require the organizer or current administrator for reads; outsider writes fail before persistence. Responses are private/no-store, including denials. Web/native comment state is remounted when the viewer or campaign changes, so a failed guest request cannot retain private comments. Fourteen API safety/privacy/block/agreement tests, two web block/logout tests and affected type/lint checks pass. This closes the direct comment-read gap, not preventive screening.

## Preventive comment/update review checkpoint

The real-admission/provider/lifecycle/action-center suite passes 10 tests; web composer/review/block coverage passes 5 tests and admin review passes 1. Native 43 tests, affected types/lint, web/admin builds, all-platform native export and mocked phone hold/resubmit flow pass; screenshot inspected. See `PUBLICATION_REVIEWS.md` for privacy, retention, exact-version/retry guarantees and remaining C09 surfaces. The stable full API baseline subsequently passed 123 files / 836 tests, without concurrent API/shared source edits. No main push, live moderation provider call or production publication occurred.

Campaign public-read correction: published-state checks now protect list/detail/slug preview, organization projections, related feeds and live-session reads. Explicit owner/admin review access remains available. Web/native caches clear on account/role/campaign changes and failed reloads; share/checkout content and SEO clear on denied refreshes. Financial records remain intact. See `CAMPAIGN_VISIBILITY.md` for verification and limits. Optional MFA/device biometrics are now implemented; broader compliance, commit and main push remain pending.


Optional authenticator MFA is implemented in member/admin/native settings and sign-in, with explicit activation, QR/manual setup, six-digit input, single-use recovery copy/download, durable attempt limits and transactional revocation/audit. API 13, web/admin 132/39 and native 47 tests pass; type/lint/build/export checks pass. See `MFA_AND_BIOMETRICS.md` for configuration and device evidence. Native biometric session protection now has protected credential storage, explicit opt-in/unlock, background/cold-start locking, privacy-cover behavior and stale-response fencing. Native 59 tests, types/lint, CocoaPods and ad-hoc signed simulator compilation pass. Isolated simulator opt-in, lock, unlock, background/resume and password fallback pass. Physical-device evidence remains pending. Admin PDF/XLSX/CSV exports are implemented across applicable data/report/detail/content pages and persisted settings, with branded Outfit fonts, current logo, colors and watermark. Pagination, current-admin access checks and cancellation are verified; 35 focused API tests, 51 admin tests, two real browser download flows, type/lint/build, rendered PDF checks and isolated clean-install dependency checks pass. See `ADMIN_EXPORTS.md`. The broader compliance goal and final commit/push remain pending.

### Account profile persistence prerequisite — 13 September 2026

Atomic submitted-field patches now preserve omitted/concurrent identity and privacy settings, fence credential changes/closure and roll back profile/account writes together. Stale generic user saves no longer restore identity. Web Settings sends ordered field-only writes, scopes work by account and restores confirmed values after failed optimistic choices. API regression: 27 tests; web: 137 tests; affected type/lint/build and mocked 390px ordering/failure flow pass. See `ACCOUNT_PROFILE_WRITES.md`. General profile publication admission and identity projection coverage remain open.

Public-identity follow-up: member/organization profiles exclude private/restricted/closed/blocked identities and no longer expose organization login emails. Comment/update lists now also suppress restricted/closed/blocked authors while preserving the separate profile-page privacy setting. Source records and finances remain intact, and client focus/periodic refreshes clear denied results. Current API/UI/build evidence and remaining initial-registration/embedded-identity scope are recorded in `ACCOUNT_PUBLICATION.md`; C09 remains in progress.

Leaderboard follow-up: account opt-outs, restrictions, closure and viewer blocks now affect list/featured/stats before ranking limits. Anonymous contributions and nonpublic-campaign donations are excluded before named totals, and the GHS board no longer sums mixed currencies. Viewer-scoped refreshes and clear scope copy are implemented; evidence and legacy/default-enrollment limits are in `LEADERBOARD_PRIVACY.md`.

Donation REST follow-up: anonymous account IDs are omitted; restricted/closed/blocked identity is redacted without changing amounts/history. Web feeds re-fetch authorized REST views on live-event notification instead of trusting event identity. API/client evidence and the remaining server-side realtime/overlay/supporter projection gaps are in `ACCOUNT_PUBLICATION.md`.

Realtime identity follow-up: current campaign-bound donation facts now replace stale event identity during live delivery and replay; restricted/closed/blocked donors and host privacy toggles are applied before output. Overlay snapshots and authenticated host REST buffers match this policy. Evidence and provider/native/legacy limits are in `REALTIME_IDENTITY.md`; creator supporter and public-attribution admission remain open.

Creator supporter projection follow-up: linked restricted/closed/blocked supporters no longer expose saved names/messages; creator totals/tip records remain unchanged. Scoped web/native refreshes preserve active tip drafts and clear denied page identity. Evidence is in `ACCOUNT_PUBLICATION.md`. Guest aliases, attribution admission and report coverage for objectionable names remain open.


## Broad regression checkpoint — 13 September 2026

After the donor-content changes, the full web suite passes 170 tests across 44 files. The full admin suite initially passed 61 tests with one 5-second profile-test timeout during concurrent app runs; rerunning the complete admin suite with one worker passes all 62 tests across 16 files without production/test changes. Logs: `/tmp/ujimora-compliance-web-regression.log`, `/tmp/ujimora-compliance-admin-regression.log`, `/tmp/ujimora-compliance-admin-regression-serial.log`.

The full API suite was started and remains under observation; no full API pass is claimed. Log: `/tmp/ujimora-compliance-api-regression.log`. Its payout integration file already reports failures, with the checkout helper submitting a public donor name without acknowledgement and receiving 428. These failures need final-result inspection and fixture/implementation reconciliation before release. Keep API/shared source stable until the running suite finishes; do not restart an active run solely because a polling call yields.


The corresponding web and admin production builds also pass after the full UI regression checks. Both retain bundle-size warnings; no build success is treated as deployment or store verification. Logs: `/tmp/ujimora-compliance-web-build.log`, `/tmp/ujimora-compliance-admin-build.log`.

API regression remains active in exec session 81527. Read-only triage locates missing donor-name acknowledgement in the payout funding helper and similar fixtures in campaign split accrual, beneficiary payout, payout idempotency, refunds and reconciliation. These are candidates, not yet repaired or proven to explain every failure. Keep the run intact and inspect final results before applying changes.


KYC backend integrity audit adds an actionable release gap: review status and user verification level are separate writes; decision persistence lacks a current-state condition and transactional audit, and active submission uniqueness is only checked before insert. See `KYC_REVIEW_INTEGRITY.md` for source evidence and required race/rollback acceptance tests. The admin UI persistence fix does not establish backend atomicity.

Full API regression completion: 900 tests passed and 46 failed across eight files (147 total). The consent/snapshot fixture corrections are now being checked together in an eight-file repair run; see `API_REGRESSION_TRIAGE.md`. Earlier running-session notes are historical, not current process state.

All eight previously failing API files now pass their 60 tests after fixture reconciliation; API type-check also passes. Both regression sessions are terminal. This resolves the observed full-run failures with focused evidence, not a fresh single-run 946-test claim. KYC backend transaction/information-request work and other documented requirements remain open.


## Cross-app regression checkpoint — 13 September 2026

After the KYC/KYB, expiry and automatic payout changes, full client suites pass: mobile 81 tests/21 files, web 178 tests/47 files, admin 67 tests/17 files. Logs `/tmp/ujimora-compliance-mobile-regression.log`, `/tmp/ujimora-compliance-web-regression.log`, `/tmp/ujimora-compliance-admin-regression.log`; their processes are terminal. These are automated source/test checks, not signed-device or store approval evidence.

The new full API regression is live in session 70687 with log `/tmp/ujimora-compliance-api-final-regression.log`. Keep API/shared source unchanged until its terminal result, inspect any failures and reconcile this checkpoint afterward. Earlier focused passes do not establish a full API pass. The full original compliance scope, final builds/exports, provider/device/legal gates and authorized commit/push remain pending.


## Current mobile dependency diagnostics — 13 September 2026

Expo Doctor completed successfully with 20/20 checks against the current mobile checkout; `/tmp/ujimora-compliance-expo-doctor.log`, process terminal. The store checklist now records this current evidence rather than referring only to the August result. Final isolated clean install, signed binary inspection, physical-device tests and configured provider/store-console evidence remain open. Fresh JavaScript exports and client tests are recorded separately; none prove external approval.

Full API regression is still active in session 70687. Continue observing that process to terminal before changing API/shared source.


## Full API regression completed — 13 September 2026

Session 70687 finished with exit 0: **989 tests across 150 files passed**, duration 684.07 seconds. Authoritative log `/tmp/ujimora-compliance-api-final-regression.log`. API/shared source was held unchanged throughout the run; the earlier running checkpoints are historical and the source freeze is now lifted. This replaces the previous incomplete full-run evidence with a complete passing suite for this checkpoint.

Together with the separately recorded mobile/web/admin test suites, web/admin builds, mobile JavaScript exports and Expo Doctor, this is a broad engineering regression checkpoint. It does not close the explicit remaining campaign/payout verification consumers, policy/provider/legacy data gaps or signed-device, legal and store-console gates. Do not infer release approval, legal certification or readiness to push a completed compliance release from test success alone. Continue the current-evidence campaign eligibility work and remaining requirement ledger.


## Expanded regression and primary-source refresh — 13 September 2026

Full API regression started after the current public-verification and withdrawal transaction/destination/fee changes. Session 5623 is active, log `/tmp/ujimora-compliance-api-expanded-regression.log`; source must remain unchanged until terminal. This is not a passing full-suite checkpoint yet. The earlier 989-test full regression predates these changes.

Current primary pages rechecked: [Google target API policy](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en) requires API 36 for new ordinary mobile apps/updates; [Apple review guidelines](https://developer.apple.com/app-store/review/guidelines/) retain the approved-nonprofit/Apple Pay conditions for direct fundraising; [BoG virtual assets](https://www.bog.gov.gh/virtual-assets/) identifies Act 1154 registration/licensing supervision. These confirm, rather than discharge, C11/C12/C18 gates. Generated signed artifacts, actual Apple fundraising classification and Ghana operator/provider authorization remain unverified.

Current code inspection finds crypto availability configured by `CRYPTO_PAYMENTS_ENABLED === 'true'` in `apps/api/src/infrastructure/config/index.ts`. That default-off capability flag is not regulatory evidence. Next crypto audit must trace all capability/admin overrides and applicable operator/provider permissions; no production configuration was read or changed.

C13 follow-up evidence: campaign allowances and creation/review transactions now consume current verification; organization/member public badges and account aggregate status handle expiry/supersession. C14 follow-up evidence: creator withdrawal replay ownership, bank/wallet final authorization, transactional reservations, exact saved bank destination and final fee checks with existing-policy write conflicts now have focused tests. See `KYC_REVIEW_INTEGRITY.md` for exact evidence and unresolved missing-policy insertion, other consumers and applicability limits. C07/C08 processor erasure, category retention and complete data-rights fulfillment remain open; the recent payment changes do not resolve those requirements.


## Expanded API regression result and corrections

Session 5623 completed with 1,023 passes and four failures across 151 files (628.77s). The failures were in campaigns/short-links multi-campaign fixtures that set historical levels without current KYC evidence. Fixtures now include current identity approval so those tests reach their intended plan-cap, pagination and slug assertions; production eligibility rules were not weakened. All 27 tests in those two files pass (`/tmp/ujimora-expanded-regression-fixes.log`, session 90875 exit 0). Source freeze lifted. The subsequent crypto recovery fix and six-test/type/lint checkpoint are in CRYPTO_RELEASE_GATES.md. A new full passing regression remains required; do not sum focused reruns into a claimed full-suite pass.


## Engineering publication checkpoint — preparation

The user requested a concrete path to commit/push. This checkpoint publishes reviewed engineering progress while the full compliance goal stays active. It does not mark the 15 engineering/audit areas or five external gates complete, authorize production financial activity, or claim store/legal approval.

Prepublication checks: all seven workspace type/lint tasks pass; web/admin/marketing production builds pass (large-bundle warnings remain); dependency patch tests pass; admin 69, mobile 81 and shared UI 24 tests pass. Web run returned 171 passes/seven failures under concurrent load; full one-worker rerun session 45008 is pending. Full API session 48519 is pending, log `/tmp/ujimora-publish-api-tests.log`. Do not claim final passes until terminal. Staging excludes local launch settings, root DOCX source/draft documents, output/ and personal image. Narrow credential-pattern scan found no matches in selected code/docs. Patch files retain upstream whitespace under .gitattributes; staged whitespace check passes.


## Incremental publication authorization

The user explicitly requested fixing and pushing incrementally instead of waiting for every compliance item. This engineering checkpoint is therefore being committed with the broader goal still active and all unresolved requirements retained. The web one-worker rerun completed with 178/178 tests across 47 files (`/tmp/ujimora-publish-web-serial-tests.log`, session 45008 exit 0). All seven type/lint tasks, admin 69, mobile 81, shared UI 24 tests, web/admin/marketing builds and dependency-patch checks pass. Full API regression session 48519 remains active at commit preparation; its result is not claimed as passing. The previous full run's four fixture failures have a 27-test passing correction checkpoint, and current crypto changes have eight passing integration tests. Follow-up verification/fixes will be pushed independently.


## Published engineering checkpoint

Commit `fca28d3b47a3dc29a996451700a36e99f0162b23` was pushed to main and verified by `git ls-remote`; tracked worktree was clean after publication, with the excluded personal/draft files remaining untracked. [GitHub CI run 34745173463](https://github.com/stanleyHayes/ubuntufund/actions/runs/34745173463) has passed clean dependency installation, dependency-security-patch checks and MongoDB replica-set startup. The remaining CI steps and local full API session 48519 are still running; no final success is claimed. This adds clean-install evidence but does not establish signed native build, provider authorization or regulatory/store approval.


## Full API pass and CI execution correction

Local full API regression session 48519 FINISHED exit 0: 1,029 tests across 151 files, 1091.24 seconds, `/tmp/ujimora-publish-api-tests.log`. API/shared source remained unchanged during the run. Source freeze lifted. This is a full passing engineering regression for published application code in fca28d3, not a release approval.

GitHub CI run 34745173463 failed in Test after successful clean install, patch checks, replica-set setup, lint and types. The reported failure was the native organization KYC interaction test exceeding its default five-second timeout while Turbo ran package suites concurrently; admin KYC tests were also under load. CI now uses `turbo test --concurrency=1`; web/admin/mobile Vitest configs restrict CI to one worker, and admin/mobile receive the existing web 30-second test/hook ceiling. No assertions were removed or weakened. Complete validation under CI=true passes web 178/47 files, admin 69/18 files and mobile 81/21 files (`/tmp/ujimora-ci-serialized-client-tests.log`, session 78469 terminal exit 0). Hosted CI must still verify the next commit.

## Full web browser checkpoint — 13 September 2026

All 64 web Playwright cases pass with one worker against an isolated seeded local API (session 32055, terminal exit 0, 2.7 minutes; `/tmp/ujimora-web-full-browser-tests.log`). Registration fixtures traverse the actual wizard and expressly accept the required agreements. KYC address coverage now includes mandatory private ID front/back and selfie references and tests rejection when evidence is absent; GPS substitutes only address proof. The wallet rejection test uses the current action and requires its seed instead of skipping. These checks combine real local auth/wallet flows with explicitly mocked provider/feature scenarios; they do not establish live money movement, external erasure, signed-device behavior, hosted-main success or legal/store approval. Nonfatal unread-count mock warnings remain recorded for follow-up.

## Current publication and withdrawal regression completed — 13 September 2026

Session 3544 finished with exit 0: **1,078 tests across 154 files pass**, 966.64 seconds (`/tmp/ujimora-api-current-publication-regression.log`). API/shared source remained unchanged throughout. This run includes index readiness, missing withdrawal-policy serialization and campaign-update final authorization/publication transactions through d5d4aac. The earlier donation-content-gate timeout did not recur; its original cause remains unproven, and no assertions were weakened. The API source freeze is lifted.

This establishes a complete passing API regression for that source, not signed-native/provider/legal/store approval or completion of the remaining requirement ledger.
