/// <reference types="vite/client" />
// Types `import.meta.env` for components consumed as source by the Vite apps
// (web / admin / marketing). References the same declaration those apps use, so
// there is never a conflicting `ImportMeta.env` merge when this package's source
// is compiled inside a consuming app.
