# Production build values (public — client bundle, no secrets).
# API is routed by vercel.json rewrite → https://ujimora-api.onrender.com
VITE_API_URL=/api/v1

# --- Cloudinary (unsigned image uploads — public values, safe to commit) ---
VITE_CLOUDINARY_CLOUD_NAME=dvoqbonr2
VITE_CLOUDINARY_UPLOAD_PRESET=ujimora
