# Production build values (public — client bundle, no secrets).
# API is routed by vercel.json rewrite → https://ubuntu-fund-api.onrender.com
VITE_API_URL=/api/v1
# Replace with the deployed donor web-app URL (Vercel project domain).
VITE_WEB_APP_URL=https://ubuntufund.vercel.app
